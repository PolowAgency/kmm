import FootprintPanelInner from './FootprintPanelInner';

/** Point d'entrée natif — voir MarketLensCanvas.tsx pour le même choix de séparation. */
export default function FootprintPanel() {
  return <FootprintPanelInner />;
}
